/*
let accumulador = 0;

console.time("for");
for (let i = 0; i < 5000; i++) {
  accumulador += i;
}
console.timeEnd("for");
console.log(`El resultado del acumulador es ${accumulador}`);

*/
/*

const // variables globales (vairables que no pueden cambiar)
let // variables locales (que corren dentro de una función... o que pueden cambiar)

*/

const Persona = { nombre: "Jorge", rol: "profesor" };
console.dir(Persona);
